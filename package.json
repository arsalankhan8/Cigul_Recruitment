{
  "name": "cigul-recruitment",
  "private": true,
  "scripts": {
    "build": "npm --prefix backend install && npm --prefix frontend install && npm --prefix frontend run build && rm -rf backend/dist && cp -r frontend/dist backend/dist",
    "start": "node backend/server.js"
  }
}
